---
- name: Install OpenMW (Morrowind engine)
  hosts: localhost
  connection: local
  gather_facts: true
  roles:
    - install_openmw
    - { role: desktop_apps, tags: [desktop] }
    - { role: verify, tags: [verify] }
